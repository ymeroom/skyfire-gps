# SkyFire GPS 測試與驗證報告

- 測試日期：2026-08-18（Asia/Taipei）
- 原始碼提交：`57d3526ff33233e73ff72cad7d80c78f461a7039`
- 測試對象：`index.html`、`video.html`、`camera.html`、核心 JavaScript 模組、公開第三方 API、GitHub Pages 部署
- 結論：**台灣地區主要流程可用；產品已收斂為台灣限定，可信模型驗證管線仍需修正。**

## 0. 台灣限定範圍更新

依產品決策，本地版本已移除全球預報定位與文案，改為台灣限定：

- 新增 `TaiwanScope`：台灣本島使用多邊形邊界，澎湖、金門、馬祖、烏坵、綠島與蘭嶼使用離島服務區。
- 東京、倫敦、上海、廈門、廈門東岸、台灣海峽測試點及無效座標會被拒絕。
- 地圖鎖定台灣視角，主頁 GPS、相機 GPS、舊快取與地圖點擊都會經過同一套台灣範圍驗證。
- 主頁、PWA、README、相機與短影音 EP4 均已改為台灣定位。

因此 QA-001 的跨國時區問題已透過產品範圍收斂排除於使用者流程之外；底層演算法仍保留原始能力，但介面不再宣稱或接受全球地點。此更新尚未部署至 GitHub Pages。

## 1. 執行摘要

現有 6 組 Node 測試全部通過。線上主頁能取得真實 Open-Meteo 7 日／168 小時資料，逆地理編碼能回傳「臺北市 信義區」，主頁時段切換、氣象沙盒、20 景點篩選、短影音播放與相機合成都可操作。

本輪原始稽核發現 2 個高優先缺陷、3 個中優先缺陷；台灣限定更新後 QA-001 已不再屬於產品支援範圍：

1. **RESOLVED BY SCOPE：全球地點的日出／日落顯示問題已由台灣限定輸入防線排除。**
2. **P1：光學驗證失敗時會用靠近預測值的隨機數當 ground truth，仍標記為 CIELAB/HSV 實測並寫回資料庫。**
3. **P2：主頁手機版互動地圖區造成 557px 文件寬度，390px 手機會水平捲動。**
4. **P2：地圖標記與相機快門的鍵盤／螢幕閱讀器語意不足，且相機頁禁止縮放。**
5. **P2：公開頁面缺少 CSP、Referrer-Policy、Permissions-Policy 等防禦性安全政策。**

## 2. 測試矩陣與結果

| 層級 | 測試內容 | 結果 | 證據摘要 |
|---|---|---:|---|
| Unit | SolarCalc、SkyFireEngine、WeatherService 資料處理、20 景點資料、DOM 綁定、TaiwanScope | PASS | `node tests/run-all-tests.js`，6/6 suites 通過 |
| Scope regression | 本島與離島正例；台灣海峽、東南外海、廈門東岸反例；相機 GPS 標籤 | PASS | 範圍外座標回傳 `false`／`null` |
| Integration | 真實 Open-Meteo API | PASS | `isSimulated=false`、7 日、168 小時、日落分數 33 |
| Integration | BigDataCloud 逆地理編碼 | PASS | 台北座標回傳「臺北市 信義區」 |
| E2E | 主頁載入預報 | PASS | 線上狀態「已更新」、7 張日預報卡、日出 05:30、日落 18:27 |
| E2E | 今日日落／明日日出／明日日落切換 | PASS | 分數依序可切換，標籤日期與時段同步更新 |
| E2E | 氣象沙盒極端條件與套用 | PASS | 理想值 95 分；高／中雲 0、低雲 100 後為 5 分，能套用到主畫面 |
| E2E | 全台／北部／東部景點篩選 | PASS | 20／5／3 張卡片 |
| E2E | 9:16 短影音影棚 | PASS | 6 集；播放後顯示暫停；EP3 切換後自動播放 |
| E2E | 拍立得相機（不授權鏡頭／GPS） | PARTIAL PASS | 4 種樣式、JPEG 預覽與重拍正常；相機頁已共用 TaiwanScope；未測真實鏡頭、GPS、系統分享 |
| Responsive | 主頁 390×844 | FAIL | viewport 375px 時 document scrollWidth 557px |
| Responsive | `video.html`、`camera.html` 390×844 | PASS | 無水平溢出 |
| Load smoke | 主頁 30 次 GET、concurrency=5 | PASS | 30/30 HTTP 200；p50 87ms、p95 282ms、max 355ms、0 error |
| Security static | 密鑰／token、危險 eval、第三方資源完整性 | PASS WITH NOTES | 未發現密鑰；無 `eval`；Leaflet 1.9.4 有 SRI |
| Accessibility | 語意控制項、縮放、名稱 | FAIL | 地圖按鈕只有 emoji 名稱；快門是 `div`；相機頁禁用 zoom |

## 3. 缺陷詳情

### QA-001 — RESOLVED BY SCOPE：超出台灣支援範圍的時區顯示問題

重現：同一個倫敦日出／日落 Date 物件在不同執行環境格式化。

| 裝置／程序時區 | 顯示日出 | 顯示日落 |
|---|---:|---:|
| UTC | 04:48 | 19:23 |
| Asia/Taipei | **12:48** | **03:23** |
| Europe/London | 05:48 | 20:23 |

目標地點為倫敦時，台灣使用者會看到台灣時區的 12:48／翌日 03:23，不是倫敦當地的 05:48／20:23。

根因：

- `SolarCalc.formatTime()` 呼叫 `toLocaleTimeString()` 時未提供目標地點 `timeZone`，因此使用瀏覽器時區。
- Open-Meteo 使用 `timezone=auto` 回傳目標地點的無 offset 時間字串，`new Date(timeStr)` 又會把它當成裝置本地時間解析。
- 現有全球測試只檢查 Date 存在與方位角範圍，沒有斷言目標地點當地時間；測試輸出其實已出現倫敦「12:48 / 03:23」但仍判定 PASS。

建議：保留 API 回傳的 `timezone`／`utc_offset_seconds`，將時間正規化成 UTC，再用 `Intl.DateTimeFormat(..., { timeZone })` 顯示；新增「裝置時區 ≠ 目標地點時區」的回歸測試。

### QA-002 — P1：驗證失敗時製造隨機 ground truth

重現：`node scripts/score-ground-truth.js` 在沒有 `python` 命令的環境中輸出 Python 失敗，隨後執行：

```js
const noise = Math.round((Math.random() - 0.45) * 12);
const gtScore = Math.max(5, Math.min(100, predScore + noise));
```

接著仍寫入：

- `status: "verified_completed"`
- `engine: "Optical Chromatic Histogram Analysis (CIELAB/HSV)"`

這會讓 ground truth 天生靠近模型預測值，造成自我驗證偏誤，並可能污染 MAE、準確率及後續自動校準。使用可用的 Python 執行相同影像得到真實結果 21 分；失敗備援本輪隨機產生 40 分，證明兩者不是同一分析。

建議：

- 使用明確可設定的 Python 命令（Windows `python`、CI `python3` 或環境變數）。
- 光學分析失敗時 fail closed：標記 `analysis_failed`，不可產生隨機真值、不可納入指標或校準。
- 若保留 demo 模式，必須寫入 `isSimulated=true` 並在統計時排除，`engine` 不得宣稱 CIELAB/HSV。

### QA-003 — P2：主頁手機版水平溢出

在 390×844 viewport 下，瀏覽器實際 layout viewport 為 375px，但頁面 `scrollWidth=557px`。主要超界元素：

- `.map-container-card`：約 538px
- `#interactiveMap`：約 536px
- `.spots-list-wrapper`：約 538px

根因是單欄 CSS Grid 的子項仍採預設 `min-width:auto`，Leaflet 內容的 intrinsic width 把 grid track 撐寬。

建議在 grid item 邊界加入 `min-width: 0; max-width: 100%`，並建立 320／375／390／768px 自動化 overflow assertion。

### QA-004 — P2：可及性缺口

- 20 個 Leaflet marker 是 `role=button`、`tabindex=0`，但無 `aria-label`／`title`，可及名稱只有「🔥」「🌅」「🔄」，使用者無法辨識景點。
- 相機快門 `#btnShutter` 使用 `div`，沒有 button 語意、可及名稱與鍵盤觸發。
- 相機頁 viewport 設定 `maximum-scale=1.0, user-scalable=no`，阻止低視力使用者縮放。
- 主頁的相機／短影音 `target="_blank"` 連結缺 `rel="noopener"`。

建議為 marker 加入完整景點名稱，快門改為 `<button>`，移除禁止縮放設定，並補齊新分頁連結的 `rel`。

### QA-005 — P2：安全標頭／政策不足

主頁 HTTP 具備 HTTPS 與 `Strict-Transport-Security: max-age=31556952`，但未觀察到：

- Content-Security-Policy
- Referrer-Policy
- Permissions-Policy
- X-Content-Type-Options

本專案是無後端靜態站，整體攻擊面有限；仍建議至少用 `<meta http-equiv="Content-Security-Policy">` 約束 script／style／img／connect／media 來源，並設定 referrer policy。GitHub Pages 不支援任意自訂回應標頭時，可在 CDN／反向代理層補齊。

## 4. 效能與負載

公開頁面單次 HTTP 量測（未節流、非 Lighthouse）：

| 頁面 | HTTP | HTML bytes | TTFB | Total |
|---|---:|---:|---:|---:|
| 主頁 | 200 | 30,367 | 0.493s | 0.523s |
| 影片影棚 | 200 | 45,525 | 0.416s | 0.501s |
| 相機 | 200 | 21,575 | 0.384s | 0.391s |

輕量負載煙霧測試：30 requests、concurrency 5，全部 200；p50 87ms、p95 282ms、平均 124ms、無錯誤。

限制：Chrome DevTools MCP 無法連到可偵錯 Chrome，因此本輪**沒有**取得正式 LCP／CLS／INP／TBT 或 Lighthouse 分數。上表不能替代 Core Web Vitals。

## 5. 發布建議

目前建議狀態：**台灣功能可用；準確率驗證宣稱仍為 Conditional No-Go**。

- 產品已定位為台灣使用，主要預報、地圖、沙盒與影棚流程可繼續使用，但仍建議先修手機地圖溢出。
- 不再對外宣稱全球任意點；若持續宣稱「實況驗證準確率」，QA-002 必須在下一版發布前修正並加入回歸測試。
- 修正後應補跑：完整 Node tests、台灣本島海岸與離島邊界回歸、390px overflow E2E、真實 Python ground-truth red/green 驗證，以及正式 Lighthouse mobile audit。
